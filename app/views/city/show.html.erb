<div class="jumbotron-container" style="background-image: url(https://images.pexels.com/photos/2255/black-and-white-city-houses-skyline.jpg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940); background-position: 15% 70%;">
  <div class="container " style="height: 300px;">
    <div class="text-center py-5 px-4">
        <h2 class="card-title h1-responsive pt-3 mb-5 font-bold"><strong><%= @city.name %></strong></h2>
    </div>
  </div>
</div>

<p>
  <ul class="nav nav-pills mb-3 justify-content-center" id="pills-tab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="pills-home-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">Infos</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-contact-tab" data-toggle="pill" href="#pills-contact" role="tab" aria-controls="pills-contact" aria-selected="false">Les potins</a>
    </li>
  </ul>
  <div class="tab-content pt-5 pl-5" id="pills-tabContent">
    <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
      <p> <strong>Ville:</strong> <%= @city.name %> </p>
      <p> <strong>Code postal:</strong> <%= @city.zip_code%> </p>
    </div>
    <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab">
    <% @city.users.each do |user| %>
      <% user.gossips.each do |gossip| %>
        <h4><%= gossip.title %></h4>
        <p><%= gossip.content %></p>
        <% if current_user%>
        <%= button_to 'like', gossip_likeys_path(gossip), method: :post, class:"btn btn-outline-info btn-sm" %>
          <p class="p-2"> <%= gossip.likeys.count %> <i class="fas fa-heart"></i></p>
        <%end%>
        <%= link_to "Voir le potin", gossip_path(gossip.id) %><br><br><br><br>
      <% end%>
    <% end%>
    </div>
  </div><br><br><br><br><br><br>
